function parseDependencies(packageData, lockData) {
  const dependencies = {};

  // ...parse packageData and lockData to build the dependencies object...

  return dependencies;
}

module.exports = {
  parseDependencies
};
